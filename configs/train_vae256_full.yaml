# Full 256px β=1 VAE (Stage 4 / sharpness). Native resolution, roomy latent, wider model,
# full-ish training — the "better-resolved (still soft) maps" path.
loss: vae
img_size: 256
latent_dim: 48
channels: 64
lr: 0.0005
batch_size: 96
epochs: 60
cache: false
num_workers: 12
betas: [1]
seeds: [0]
